[tox]

[base]
deps =
    pytest-flakes
    pytest-pep8

[testenv]
deps = {[base]deps}
commands =
    py.test                     \
    --flakes                    \
    --pep8                      \
    --basetemp={envtmpdir}      \
    --confcutdir=..             \
    []
